---
phase: 01-secure-week-configuration
plan: 03
type: execute
wave: 3
depends_on:
  - 01-02
files_modified:
  - src/domain/week.ts
  - src/app/router.ts
  - src/app/App.ts
  - src/ui/html.ts
  - public/styles.css
  - tests/core.test.mjs
  - tests/integration.test.mjs
autonomous: true
requirements:
  - WEEK-01
  - WEEK-02
  - WEEK-03
  - WEEK-04
  - TIME-01
  - TIME-02
  - FORM-01
  - FORM-02
  - FORM-03
must_haves:
  truths:
    - "D-10: Preview week atomically replaces all editing controls with the exact public-facing configuration projection, and Return to editing restores the unchanged local draft, expanded cards, and focus state."
    - "If preview requires public configuration resolution, its control is disabled with Loading preview… until the public view replaces editor controls atomically."
    - "The preview is an allowlisted projection containing only theme, public description, dated ordered agenda, safe locations/links, Demo Day timing, and proposal-field labels/required markers."
    - "D-08/D-11: The one-column setup workspace keeps its readiness anchor, inline validation, first-invalid focus, and readiness-gated Create week/Publish changes controls when switching between edit and preview."
    - "Before initial configuration resolution the UI shows a non-editable Loading week configuration… panel; load failure exposes Retry; an unassigned identity sees only This identity is not assigned a week to configure."
    - "Signing, relay, and conflict failures preserve the local draft presentation, show the approved publication error, and expose Try again; success shows Week published. Intake remains closed."
    - "Empty, partial, populated, overflow, zero/one/many, and long-text editor states retain the approved copy, controls, ordering, and vertical growth defined by the UI contract."
    - "Below 660px the workspace remains one column with full-width stacked actions, no required horizontal scrolling, 44px targets, semantic grouping, programmatic labels, linked errors, and a visible cyan focus ring."
    - "Dynamic markup and URLs are escaped or normalized throughout the editor, notices, and preview, including hostile theme, description, activity, location, link, and field-label fixtures."
    - "The build and local server continue to expose the Phase 1 development path, while Plan 04's signed Nostr boundary remains the authoritative persistence path."
  artifacts:
    - path: "src/domain/week.ts"
      provides: "Exact public configuration projection with an explicit field allowlist"
      exports: ["publicWeekProjection"]
    - path: "src/app/router.ts"
      provides: "Recognized assigned-week setup route whose assignment is resolved from active identity"
      exports: ["AppRoute", "parseRoute"]
    - path: "src/app/App.ts"
      provides: "Preview/edit state plus complete loading, error, access, readiness, notice, and focus rendering"
    - path: "src/ui/html.ts"
      provides: "Escaped public agenda/form projection and accessible status/error helpers"
    - path: "public/styles.css"
      provides: "Responsive preview, status, readiness, loading/error, wrapping, and focus rules"
    - path: "tests/core.test.mjs"
      provides: "Projection allowlist, route fallback, and hostile content/URL assertions"
    - path: "tests/integration.test.mjs"
      provides: "Preview restoration and complete controller UI-state assertions"
  key_links:
    - from: "src/app/router.ts"
      to: "src/domain/cohort.ts"
      via: "the setup route resolves the current identity's assignment rather than accepting captain authority from hash state"
      pattern: "weekForCaptain"
    - from: "src/app/App.ts"
      to: "src/domain/week.ts"
      via: "preview renders only publicWeekProjection(draft), while validateWeekConfiguration continues to gate publication"
      pattern: "(publicWeekProjection|validateWeekConfiguration)"
    - from: "src/app/App.ts"
      to: "src/ui/html.ts"
      via: "all dynamic editor, status, error, and preview values pass through escaped helpers"
      pattern: "(escapeHtml|escapeAttr|field|textarea)"
    - from: "public/styles.css"
      to: "src/app/App.ts"
      via: "semantic classes implement the single-column, focus, wrapping, and sub-660px state contract"
      pattern: "(week|preview|readiness|focus-visible)"
  prohibitions:
    - statement: "Preview and public configuration must not reveal the participant allowlist, captain-only readiness state, local draft status, proposal answers, or later-phase scheduling data."
      status: "unverified"
      flagged: true
    - statement: "Preview must not contain a proposal-submission action or make intake appear open during Phase 1."
      status: "unverified"
      flagged: true
    - statement: "Loading, preview, initial-error, and unauthorized panels must not expose stale editor or publication controls."
      status: "unverified"
      flagged: true
    - statement: "Phase 1 UI controls must not rely on icon-only actions, horizontal scrolling, clipped dynamic labels, or color alone to communicate state."
      status: "unverified"
      flagged: true
---

## Phase Goal

**As a** designated captain, **I want to** configure and publish my assigned week, **so that** I can tailor it without code changes.

<objective>
Complete the D-08/D-10/D-11 public-preview and UI-state vertical slice after the signed tracer and full editor are working.

Purpose: Make the complete captain workspace safe and understandable across preview, loading, failure, unauthorized, validation, responsive, accessibility, hostile-content, and long-data conditions.
Output: Public projection, setup-route recognition, atomic preview/edit behavior, complete rendered states, responsive/accessibility styling, and focused UI/core assertions.
</objective>

## Artifacts this plan produces

- `src/domain/week.ts`: `publicWeekProjection`
- `src/app/router.ts`: assigned-week setup route variant and parser branch
- `src/app/App.ts`: preview/edit state, status chips, loading/error/access panels, and focus restoration
- `src/ui/html.ts`: safe public agenda/proposal-field projection helpers
- `public/styles.css`: responsive preview, readiness/status, loading/error, wrapping, and focus rules
- `tests/core.test.mjs`: projection allowlist, route, and unsafe content/URL cases
- `tests/integration.test.mjs`: preview restoration and full UI-state cases

## Scope Boundary

This seven-file plan owns the public projection and presentation side of the proven signed-week flow. It consumes Plan 01's captain-authorized repository contract and Plan 02's complete domain editor/readiness behavior. It does not modify event builders, event parsers, or repository persistence; Plan 04 owns their signed-revision hardening and exhaustive security matrices.

<execution_context>
@/Users/sandwich/.codex/gsd-core/workflows/execute-plan.md
@/Users/sandwich/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-secure-week-configuration/01-CONTEXT.md
@.planning/phases/01-secure-week-configuration/01-UI-SPEC.md
@.planning/phases/01-secure-week-configuration/01-01-SUMMARY.md
@.planning/phases/01-secure-week-configuration/01-02-SUMMARY.md

<interfaces>
Consume Plan 01's `ProvisionedWeek`, `WeekConfigurationV1`, week-coordinate helpers, strict week repository methods, and retained local-draft/error state.
Consume Plan 02's `validateWeekConfiguration`, readiness results, stable-ID mutations, escaped field helpers, and responsive card classes.
Keep `parseRoute` as a recognized discriminated union with an `invalid` fallback; setup authority comes from active identity plus manifest assignment, never hash payload.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Render the exact public projection and restore the editor atomically</name>
  <read_first>
  - `.planning/phases/01-secure-week-configuration/01-CONTEXT.md` for D-04, D-08, D-10, D-11, and D-12
  - `.planning/phases/01-secure-week-configuration/01-UI-SPEC.md` for Page structure, Preview, exact copy, and public-field exclusions
  - `src/domain/week.ts`, `src/app/router.ts`, `src/app/App.ts`, `src/ui/html.ts`, `tests/core.test.mjs`, and `tests/integration.test.mjs` for every edited behavior in this task
  - `src/domain/cohort.ts` and `src/nostr/repository.ts` for assignment and public configuration loading interfaces consumed unchanged
  </read_first>
  <files>src/domain/week.ts, src/app/router.ts, src/app/App.ts, src/ui/html.ts, tests/core.test.mjs, tests/integration.test.mjs</files>
  <behavior>
  - Preview contains exactly the approved public fields and no provisioning, participant, editor, readiness, proposal-answer, local-status, or intake controls.
  - Preview week swaps the entire workspace only after required public state resolves; Return to editing restores the same draft, expanded cards, and focus target.
  - Route recognition never accepts a captain pubkey, week coordinate, or authorization claim from user-controlled hash segments.
  - Malicious dynamic text is escaped, unsafe URL schemes do not become hrefs, and valid normalized HTTP(S) links remain descriptive and usable.
  </behavior>
  <action>
  Implement D-10 with `publicWeekProjection(configuration)`, returning only theme, public description, dated ordered Tuesday/Wednesday agenda data, escaped displayable locations and normalized links, Demo Day timing, and proposal-field labels/required markers. The projection must omit the participant allowlist, `base_event_id`, captain-only readiness/local state, proposal answers, scheduling decisions, archive/clone metadata, and any secret material.

  Add a recognized assigned-week setup route per D-04/D-08, but resolve the slot from the active identity through `weekForCaptain`; hash state may select only the known setup view and must not supply author, coordinate, or captain authority. Keep the `invalid` route fallback.

  Add atomic Preview week and Return to editing transitions in `DemoDayApp`. Preserve the exact local draft, expanded-card map, readiness state, and focus target while preview is active. If public configuration resolution is required, disable the preview control, show `Loading preview…`, and replace editor controls only after resolution succeeds. Preview must not validate, sign, publish, or expose a proposal-submission action.

  Extend `src/ui/html.ts` through existing `escapeHtml`, `escapeAttr`, and normalized-link patterns. Add focused core tests that enumerate the projection's allowed keys, reject excluded keys, cover valid/invalid route forms, and exercise hostile strings/URLs; add controller integration assertions for atomic preview entry/exit and restored draft/card/focus state.
  </action>
  <acceptance_criteria>
  - Source: D-04/D-08/D-10/D-11 are visible in projection, route, and controller behavior without moving authorization into route state.
  - Behavior: Preview week and Return to editing preserve draft, expansion, readiness, and focus state byte-for-byte while swapping the visible workspace atomically.
  - Security: allowlist tests enumerate approved projection keys; hostile markup remains text and non-HTTP(S) URLs are not anchors.
  - Test: route tests cover the recognized setup route and invalid fallback, while integration tests cover preview loading, entry, exit, and state restoration.
  </acceptance_criteria>
  <verify>
    <automated>npm run verify</automated>
  </verify>
  <done>The captain can enter the exact safe public preview and return to the unchanged local editor, with assignment still derived from signed identity and manifest data.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Complete loading, error, responsive, and accessible workspace states</name>
  <read_first>
  - `.planning/phases/01-secure-week-configuration/01-UI-SPEC.md` for all 12 UI Considerations, Responsive/accessibility behavior, typography, spacing, color, and exact copy
  - `src/app/App.ts`, `src/ui/html.ts`, `public/styles.css`, `tests/core.test.mjs`, and `tests/integration.test.mjs` for every edited behavior in this task
  - `src/domain/week.ts` for preview/readiness data already established by Task 1 and Plan 02
  </read_first>
  <files>src/app/App.ts, src/ui/html.ts, public/styles.css, tests/core.test.mjs, tests/integration.test.mjs</files>
  <behavior>
  - Initial load, load failure, unassigned access, local changes, ready/not-ready, preview loading, busy publication, publish failure, retry, and success states each show the approved copy and only allowed controls.
  - Needs attention navigation expands the affected panel/card and focuses the first invalid field after a blocked publish.
  - Empty, partial, populated, overflow, zero/one/many, and long-text fixtures retain semantic source order, controls, and readable wrapping.
  - At widths below 660px, panels remain one column and action rows stack full width without horizontal scrolling.
  - Native controls have programmatic labels, linked help/errors, hierarchical headings, semantic groups, 44px targets, polite live notices, and visible cyan focus treatment.
  </behavior>
  <action>
  Finish the D-08/D-11 workspace header and state rendering: derived Week number/date range, read-only Atlantic/Madeira, assigned captain display, and Draft / Setup published — intake closed / Changes ready to publish chips. Render initial `Loading week configuration…`, load-failure Retry, the unassigned access-only panel, `Changes saved locally`, readiness blockers, publication busy state, the approved publication error with Try again, and `Week published. Intake remains closed.`. Preserve the existing local draft and expanded state across publication errors; Plan 04 will harden the coordinating signed operation without changing these presentation contracts.

  Implement every UI-SPEC state row: empty Tuesday/Wednesday groups, empty proposal form, partial invalid sections, typical and large populated card stacks, vertical overflow, zero/one/many movement boundaries, long wrapping content, preview loading, unauthorized access, and fixed short interactive labels. Needs attention actions must expand the affected section/card and focus the first invalid field. Loading, preview, initial error, and unauthorized panels expose no stale editor or publication controls.

  Extend semantic sections/fieldsets/headings, programmatic labels and `aria-describedby`, polite live notices, visible `:focus-visible` cyan ring, 44px targets, `overflow-wrap:anywhere`, the declared 4px spacing scale, 16/14/22/48px typography, existing semantic colors, and sub-660px full-width stacked actions. Preserve one-column source order and avoid horizontal scrolling or clipped dynamic text.

  Add focused render-state assertions for exact copy/control presence and absence, first-invalid focus, empty/partial/populated/overflow/zero-one-many/long-text fixtures, and safe hostile content. Build the app after the tests so stylesheet and TypeScript integration are both exercised.
  </action>
  <acceptance_criteria>
  - Source: every UI-SPEC `## UI Considerations` row is represented in the coverage lift below and implemented across Plans 01–03.
  - Behavior: each loading, error, unauthorized, empty, partial, populated, overflow, zero/one/many, and long-text fixture renders the approved copy and allowed controls.
  - Accessibility: errors and help are programmatically linked; first-invalid focus, live notices, 44px controls, semantic grouping, and visible keyboard focus are present.
  - Responsive: the sub-660px build retains source order and stacked full-width actions without required horizontal scrolling.
  - Test: `npm run verify` covers the controller/UI fixtures and `npm run build` succeeds.
  </acceptance_criteria>
  <verify>
    <automated>npm run verify && npm run build</automated>
  </verify>
  <done>The complete captain workspace has safe, accessible, responsive, and test-backed presentation for every approved Phase 1 UI state.</done>
</task>

</tasks>

## UI Consideration Coverage Lift

| UI-SPEC row | Owning plan(s) | Executable evidence |
|---|---|---|
| Empty Tuesday/Wednesday collections | 01-02, 01-03 | Domain/editor fixtures plus final state rendering |
| Loading assigned week/public configuration | 01-01, 01-03 | Tracer load path plus non-editable exact-copy state |
| Initial-load and signing/publish errors | 01-03, 01-04 | Error/Retry/Try again render tests plus signed-operation retention tests |
| Populated activity/proposal collections | 01-02, 01-03 | Ordered-card editor tests plus final render states |
| Partial invalid forms | 01-02, 01-03 | Readiness validation plus first-invalid UI behavior |
| Overflow collections | 01-02, 01-03 | Vertical card-stack and no-horizontal-scroll rules |
| Zero/one/many collections | 01-02, 01-03 | Empty, boundary-disabled, and ordered multi-card fixtures |
| Long editor/status text | 01-02, 01-03 | Retained values, wrapping, and accessible identity-name behavior |
| Empty proposal form | 01-02, 01-03 | Blocked readiness plus approved empty-state rendering |
| Preview loading | 01-03 | Disabled toggle, Loading preview…, atomic replacement |
| Authorization boundary | 01-01, 01-03, 01-04 | Captain tracer, access-only panel, and defense-in-depth signed checks |
| Fixed short interactive labels | 01-02, 01-03 | Native text controls with dynamic values kept in wrapping content |

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| hash route / active identity -> setup UI | User-controlled navigation requests an assigned captain view, but assignment is derived from identity plus manifest. |
| local draft -> public preview | A richer editor model is projected into intentionally public configuration fields. |
| untrusted dynamic data -> innerHTML renderer | Relay/configuration strings and URLs enter the full-root HTML renderer. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-13 | Elevation of privilege | `src/app/router.ts` / `src/app/App.ts` setup view | high | mitigate | Derive the visible slot from active identity and validated manifest, expose only an access panel when unassigned, and continue relying on Plan 01/04 non-UI authorization for mutations. |
| T-01-16 | Tampering | `src/ui/html.ts` editor/preview rendering | high | mitigate | Escape every dynamic string/attribute and accept href values only through HTTP(S)-only normalization, with hostile fixtures. |
| T-01-17 | Information disclosure | `publicWeekProjection` | high | mitigate | Construct an explicit projection allowlist and test absence of allowlist, draft/readiness, proposal answers, scheduling decisions, and secret material. |
| T-01-18 | Denial of service | load/preview/render state transitions | medium | mitigate | Bound inputs through existing domain validators, disable duplicate transitions, retain retryable state, and render non-editable loading/error panels. |
| T-01-20 | Spoofing | UI status and publication notices | low | accept | Status is presentation only and never grants authority; Plan 04 verifies signed repository state before publication becomes authoritative. |
</threat_model>

<verification>
- `npm run verify` and `npm run build` pass.
- Projection tests prove only approved public keys survive and unsafe markup/URLs are neutralized.
- Route and controller tests prove hash state cannot select captain authority and preview restores the exact editor state.
- Render tests cover all 12 UI Considerations, approved copy/control states, focus/error associations, and responsive class contracts.
</verification>

<success_criteria>
- D-08/D-10/D-11 are complete as observable, accessible captain-workspace behavior.
- WEEK-01 through WEEK-04, TIME-01/TIME-02, and FORM-01 through FORM-03 are represented accurately in the public projection and UI states.
- All 12 UI Considerations have explicit ownership and executable evidence without modifying the signed Nostr boundary in this plan.
- Plan 04 can harden signed revision coordination against stable presentation/projection contracts.
</success_criteria>

<output>
Create `.planning/phases/01-secure-week-configuration/01-03-SUMMARY.md` when done.
</output>
